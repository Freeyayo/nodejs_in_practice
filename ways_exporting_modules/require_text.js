const groups = require("./groups"); //it only works if there's index.js file exists in "group" folder

//besides, we can create package.json file to modify the "main" to point another js file as default 

groups.one();

groups.two();